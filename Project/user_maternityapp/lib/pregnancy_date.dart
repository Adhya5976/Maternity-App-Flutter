import 'package:flutter/material.dart';

class PregnancyDate extends StatelessWidget {
  const PregnancyDate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}